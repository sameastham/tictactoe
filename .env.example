# SQLite database file path (default: data/app.db)
DB_PATH=data/app.db
# "anthropic" | "fixture". Default: anthropic if ANTHROPIC_API_KEY is set, else fixture.
MODEL_PROVIDER=
# Anthropic model id override (default: claude-opus-5)
MODEL_ID=
ANTHROPIC_API_KEY=
